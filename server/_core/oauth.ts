// OAuth is now handled by Supabase Auth on the frontend.
// This file is kept as a placeholder to avoid import errors.
import type { Express } from "express";

export function registerOAuthRoutes(_app: Express) {
  // no-op
}
